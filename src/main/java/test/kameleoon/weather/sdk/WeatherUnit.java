package test.kameleoon.weather.sdk;


public enum WeatherUnit {
    /**
     * Temperature in Kelvin is used by default=standard
     */
    STANDARD,
    /**
     * For temperature in Celsius use units=metric
     */
    METRIC,
    /**
     * For temperature in Fahrenheit use units=imperial
     */
    IMPERIAL
}
